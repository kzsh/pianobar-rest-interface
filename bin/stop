#!/bin/bash
SCRIPT_DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )

kill $(cat $SCRIPT_DIR/../tmp/server.pid.txt) && rm $SCRIPT_DIR/../tmp/server.pid.txt
kill $(cat $SCRIPT_DIR/../tmp/pianobar.pid.txt) && rm $SCRIPT_DIR/../tmp/pianobar.pid.txt
